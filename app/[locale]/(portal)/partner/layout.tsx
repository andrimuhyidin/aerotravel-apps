/**
 * Partner Layout
 * Layout for B2B partner portal
 * Features: Partner branding, wallet balance display
 */

import React from 'react';

export default function PartnerLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
