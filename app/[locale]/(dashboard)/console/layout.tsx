/**
 * Console Layout
 * Layout for internal admin console
 * Features: Sidebar navigation, dense UI, role-based access
 */

import React from 'react';

export default function ConsoleLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
