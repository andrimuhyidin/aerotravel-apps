#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "🔍 Running pre-commit checks..."

# Run type check (non-blocking for now)
echo "📘 Type checking..."
pnpm type-check || echo "⚠️  Type check has errors (non-blocking)"

# Run linter
echo "🔧 Linting..."
pnpm lint --fix

# Run affected unit tests
echo "🧪 Running tests..."
pnpm test:unit --run --changed || echo "⚠️  Some tests failed (non-blocking)"

echo "✅ Pre-commit checks complete!"
