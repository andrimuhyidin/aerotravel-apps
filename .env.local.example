# Add these to your .env.local file:

# Cron Secret (for securing cron endpoints)
CRON_SECRET=your-secret-key-here-change-in-production

# Base URL for booking links in emails
NEXT_PUBLIC_BASE_URL=https://app.aerotravel.id
# For local development:
# NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Vercel Plan (hobby or pro)
# Controls cron job configuration during build
# hobby = 1 combined daily cron (Vercel Hobby plan limit)
# pro = 11 individual crons with different schedules
VERCEL_PLAN=hobby
